# [CFG-001] 实现共享配置快照模型

## 1. 任务元信息
- 编号：`CFG-001`
- 状态：`IN_PROGRESS`
- 优先级：`高`
- 当前阶段：`M2 POC 验证`
- 前置依赖：`无`
- 关联任务：`POC-002`、`POC-003`、`APP-001`
- 最后更新时间：`2026-04-21 11:44 CST`
- 最后执行者：`Codex`
- 最后安全检查点：`共享模型与默认常量已存在于仓库中，任务文件已补建；真实 App Group 快照读写仍待继续推进。`

## 2. 任务目标

将 LiteShunt 的共享配置收敛为可版本化、可原子替换、可安全回退的快照结构，并打通宿主应用与扩展之间的共享读写基础。

## 3. 背景

`TECHNICAL_DESIGN.md` 已明确配置共享主线为 `App Group + JSON 配置快照`。当前仓库已有共享模型、默认值和基础测试，但尚未完成真实共享读写与回退链路。如果该任务长期停留在隐式状态，会影响 `POC-002` 和 `POC-003` 的可落地性，因此需要在任务体系中显式建档。

## 4. 范围

- 允许修改：
  - `Sources/LiteShuntShared/`
  - `LiteShuntShared/`
  - `LiteShuntApp/Services/`
  - `LiteShuntExtension/`
  - `Tests/`
  - `docs/ai/`
- 允许新增：
  - 配置持久化辅助类型
  - 快照回退测试
  - App Group 路径与序列化的 POC 支撑代码

## 5. 非目标

- 不扩展为宿主与扩展的常驻进程通信
- 不引入额外数据库或第三方同步组件
- 不在本任务中实现完整规则管理 UI

## 6. 前置条件

1. 共享模型定义与默认常量已与 `TECHNICAL_DESIGN.md` 基本一致。
2. 宿主应用和扩展的 App Group 标识策略已在工程中保留占位。

## 7. 完成标准

1. `LiteShuntConfig` 快照结构具备稳定版本字段和序列化入口。
2. 宿主应用可写入快照，扩展可读取快照，失败时有明确错误或回退策略。
3. 至少存在配置解析、回退或持久化相关的自动化验证。
4. 实际限制与未完成部分被记录，避免后续任务隐式依赖。

## 8. 验收方式

1. 执行 `swift test`，确认共享模型和配置相关测试通过。
2. 结合代码检查宿主侧写入与扩展侧读取入口，确认职责没有混杂。
3. 核对后续依赖任务是否能基于本任务文件快速判断可复用的现状。

## 9. 执行步骤

- [ ] 盘点现有共享模型与默认值落点
- [ ] 收敛宿主写入与扩展读取链路
- [ ] 补齐配置解析 / 回退测试
- [ ] 更新交接和验收记录

## 10. 执行现场

### 10.1 当前进度

根据 `PLAN.md`，本任务已达到约 `70%` 的阶段性进度，但缺少独立任务文件承载执行现场。

### 10.2 已完成内容

- 共享模型、默认常量和基础测试已经存在于仓库。
- 任务文件已初始化，后续可在此持续记录真实进展。

### 10.3 当前阻塞

- 真实 App Group 文件读写和回退链路的现状还未在任务文件中沉淀。

### 10.4 最新决策

- 本任务作为支撑任务保留 `IN_PROGRESS` 状态，不单独抢占当前唯一主任务位置。

### 10.5 修改文件

- `docs/ai/TASK_LIST.md`
- `docs/ai/tasks/CFG-001.md`

### 10.6 已执行命令

- `rg --files`
- 结果：已确认共享模型和测试文件存在于仓库中。

### 10.7 下一步建议

- 下一位接手者应把当前真实读写实现进度补充到本文件，再决定是否需要拆出更细的子任务。

### 10.8 接手提示

不要只看 `PLAN.md` 中的百分比推进本任务，先核对仓库里已有的共享模型、测试和宿主 / 扩展读写入口，再更新这里的动态现场。
